<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PracticumAndroidDiploma" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorOnPrimary">@color/black</item>

        <!-- Цвет области ввода текста, без hint -->
        <item name="colorPrimaryContainer">@color/light_gray</item>
        <item name="colorOnPrimaryContainer">@color/black</item>

        <!-- Secondary brand color. -->
        <item name="colorOnSecondary">@color/gray</item>

        <!-- Error color. -->
        <item name="colorError">@color/red</item>

        <!-- Customize your theme here. -->
        <item name="android:background">@color/white</item>
        <item name="blackWhiteColor">@color/black</item>


    </style>

    <style name="Theme.PracticumAndroidDiploma" parent="Base.Theme.PracticumAndroidDiploma" />
</resources>